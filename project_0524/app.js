const http = require("http")
const fs = require("fs")
const path = rerquire('path')

const sever = http.createServer((req,res)=>{
  if(req.method === "GET") {
    if(req.url === "/"){
      
    }
  }
})